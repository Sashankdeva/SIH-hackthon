# benchmarks/

Deferred past 1 Sept 2026 — see `docs/planning/PS26171_Sprint_Plan.pdf`, §1. The full
5-metric benchmark suite (visual accuracy, PII precision/recall, redaction
precision, client resource utilization, end-to-end latency) is Phase 2
work, owned by **Role 6 (Integration, Benchmark & Demo)**.

This folder is intentionally empty until then — don't build harnesses here
before the golden-path demo (extension &rarr; server &rarr; action &rarr;
verification) is reliable. See `docs/ARCHITECTURE.md` for the priority rule.
