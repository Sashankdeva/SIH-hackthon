# dashboard/

Deferred past 1 Sept 2026. This is the *full* debug/observability
dashboard (latency breakdown, PVM hit/miss, inference-mode telemetry) —
distinct from the basic privacy inspector panel, which ships this sprint
inside `extension/src/popup/` (Day 3, Role 3/Role 6).

Build here once the golden-path pipeline is reliable and there's real
telemetry worth displaying. See `docs/planning/PS26171_Sprint_Plan.pdf`
and `docs/ARCHITECTURE.md`.
